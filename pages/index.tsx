import type { NextPage } from 'next'
import { makeStyles } from '@mui/styles';
const useStyle = makeStyles((theme: any) => ({
}))
const Home:NextPage= () => {
  return (
    <div>
      indexdsdsds
    </div>
  );
};

export default Home
